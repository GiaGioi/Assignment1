<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.poly.gia_gioi.assignment">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".MhcActivity"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListUserActivity" />
        <activity android:name=".QuanLiLoaiSachActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".QuanLiSachActivity" />
        <activity android:name=".SachBanChayActivity" />
        <activity android:name=".ThongkeActivity" />
        <activity android:name=".HomeAtivity" >

        </activity>
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".HoaDonActivity" />
        <activity android:name=".ThemSachActivity" />
        <activity android:name=".HoaDonChiTietActivity" />
    </application>

</manifest>